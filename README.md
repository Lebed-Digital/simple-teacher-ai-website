# Simple Teacher AI

A small, production-ready landing page for SimpleTeacherAI.com, built with React, TypeScript, Vite, and Tailwind CSS.

## Local development

```bash
npm install
npm run dev
```

Replace the placeholder social URLs in `src/App.tsx` before launch.
